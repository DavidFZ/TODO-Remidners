package edu.square.models.mapper;

import org.apache.ibatis.session.SqlSession;
import org.apache.ibatis.session.SqlSessionFactory;

public class MapperTest {

    public static void main(String[] args) {
//        SqlSessionFactory sqlSessionFactory =new MyBatisUtil().getSqlSessionFactory();
//        try (SqlSession session = sqlSessionFactory.openSession()) {
//            ReminderMapper mapper = session.getMapper(ReminderMapper.class);
//            mapper.getAllEntities().forEach(System.out::println);
//        }

    }
}
